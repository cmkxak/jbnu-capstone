<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firsttest">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstTest">

        <!--> 회원가입 화면 <!-->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.FirstTest.NoActionBar" />
        <!--> 메인 화면 <!-->

        <!--> 로그인(메인) 화면 <!-->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstTest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--> 유저리스트 화면 <!-->
        <activity
            android:name=".UserListActivity"
            android:exported="false" />
        <!--> 유저리스트 화면 <!-->
        <activity
            android:name=".AddSignUpActivity"
            android:exported="false" />

        <!--> 실시간 모니터링 화면 및 응급 화면 <!-->
        <activity android:name=".ui.emergencylive.EmergencyLiveActivity"
            android:exported="true"
            android:theme="@style/Theme.FirstTest.NoActionBar"
            />

        <!--> 환경 설정 화면 <!-->
<!--        <activity-->
<!--            android:name=".ui.setting.MySettingActivity"-->
<!--            android:theme="@style/Theme.FirstTest.NoActionBar" />-->

        <!--> FCM SERVICE <!-->
        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!--> Notification channel id for FCM SERVICE <!-->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
    </application>
</manifest>